<div id="course_<%= course.id %>" class="course col-md-4" %>
	<img src="<%= course.image %>" alt="course_<%= course.id %> image"/>
	<h3><%= course.name %></h3>
	<p><%= course.category %></p>
	<p><%= course.description %></p>
	<p><%= course.city_name %></p>
</div>
	   